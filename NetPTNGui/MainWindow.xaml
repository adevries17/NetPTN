<Window x:Class="NetPTNGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetPTNGui"
        mc:Ignorable="d"
        Title="NetPTN" Height="600" Width="800">

    <Grid Margin="8">
        <!-- Define the rows in the grid system-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Define the columns in the grid system-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Here is where the controls will be defined-->
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="5,0,0,0">
            <Label FontSize="14">IP or Hostname:</Label>
            <CheckBox>Ping -t</CheckBox>
        </StackPanel>

        <TextBox Grid.Row="0" Grid.Column="1" x:Name="QueryInput"></TextBox>

    </Grid>
</Window>
